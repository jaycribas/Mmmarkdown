.sideBar
  .appName 
    h1 mmmarkdown
    p.appDescription
      | ...the 
      span.emphasis Lobster 
      | of markdown editors (๑ > ◡ < ๑)
  span.files
    for file in files || 'a'
        .file(id=`${file}`)= file
          form.deleteFile(action=`/api/delete/${file}` method="post")
            button(type="submit") delete
    a(href="/")
      .createNew create new file
